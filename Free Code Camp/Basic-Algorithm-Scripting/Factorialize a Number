function factorialize(num) {
  
  var reduce_arr = [];
  for (var i = 1; i <= num; i++) {
    reduce_arr.push(i);
  }
  
  return reduce_arr.reduce(function(currentVal, previousVal) {
    return currentVal * previousVal;
  }, 1);
  
  
}

factorialize(5);